import { ValidationOptions } from 'class-validator';
export declare function WordCount(min: number, max: number, validationOptions?: ValidationOptions): (object: object, propertyName: string) => void;
