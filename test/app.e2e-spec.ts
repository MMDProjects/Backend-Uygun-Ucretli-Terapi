/**
 * Boilerplate test replaced: this app has no GET / route.
 * A minimal smoke test ensures the e2e jest config works.
 */
describe('App (e2e) smoke test', () => {
  it('should pass trivially (no GET / in this app)', () => {
    expect(true).toBe(true);
  });
});
